## npm i dotenv 
## create a .env file and write down mongo_db_uri
## import dotenv in server.js : require('dotenv').config()  or: const dotenv=require('dotenv'); dotenv.config()
## instal mangodb pakacge through npm : npm i mongodb ??- generaly we use mongoose but this project is of simple complexitiy we are not creating mongose and doing in this way.